@using Microsoft.AspNetCore.Identity

@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

<ul class="navbar-nav">
    @if (SignInManager.IsSignedIn(User))
    {
        @* <li class="nav-item">
    <a id="manage" class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello
    @UserManager.GetUserName(User)!</a>
    </li>
    <li class="nav-item">
    <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout"
    asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
    <button id="logout" type="submit" class="nav-link btn btn-link text-dark border-0">Logout</button>
    </form>
    </li> *@
        <a href="/Admin"
            class="text-white bg-primary-green hover:bg-primary-green-hover focus:ring-4 focus:ring-primary-green-hover font-semibold rounded-md text-sm px-[18px] py-2 focus:outline-none">Dashboard</a>
    }
    else
    {
        @* <li class="nav-item">
    <a class="nav-link text-dark" id="register" asp-area="Identity" asp-page="/Account/Register">Register</a>
    </li> *@
        <a class="text-white bg-primary-green hover:bg-primary-green-hover focus:ring-4 focus:ring-primary-green-hover font-semibold rounded-md text-sm px-[18px] py-2 focus:outline-none"
            id="login" asp-area="Identity" asp-page="/Account/Login">Login</a>
    }
</ul>
